{
  "name": "subgraph",
  "version": "1.0.0",
  "repository": "https://github.com/defichaindomains/subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --product hosted-service defichaindomains/subgraph",
    "create-local": "graph create --node http://localhost:8020/ defichaindomains/subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ defichaindomains/subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 defichaindomains/subgraph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-ts": "^0.31.0"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.60.0",
    "matchstick-as": "0.6.0"
  }
}
