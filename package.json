{
  "name": "defichaindomains-subgraph",
  "version": "1.0.0",
  "repository": "https://github.com/defichaindomains/subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --product hosted-service defichaindomains/defichain-domains-registry",
    "create-local": "graph create --node http://localhost:8020/ defichaindomains/defichain-domains-registry",
    "remove-local": "graph remove --node http://localhost:8020/ defichaindomains/defichain-domains-registry",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 defichaindomains/defichain-domains-registry",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-ts": "^0.28.1"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.37.1",
    "matchstick-as": "0.5.0"
  }
}